// Client-safe UI configuration only. AI credentials live exclusively in the
// deployed worker's OPENAI_API_KEY environment variable.
export const APP_CONFIG = {
  name: 'Interview Pilot',
  defaultQuestionCount: 5,
};
